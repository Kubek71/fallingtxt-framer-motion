import styled from "styled-components";
import { motion } from "framer-motion";
export const SvgTextBoxStyled = styled(motion.svg)`
  stroke: #fff;
  stroke-width: 2;
  stroke-linejoin: round;
  stroke-linecap: round;
`;
